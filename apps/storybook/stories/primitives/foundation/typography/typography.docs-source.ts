export const typographyRecommendedUsageCode = `import { Typography } from "@fodmapp/ui/typography";

export function Example() {
  return (
    <div className="space-y-3">
      <Typography variant="h3">Meal swap summary</Typography>
      <Typography variant="lead">
        Keep the lead concise so the detailed guidance remains easy to scan.
      </Typography>
      <Typography>
        A regular paragraph should carry the instructional body copy.
      </Typography>
    </div>
  );
}
`;
