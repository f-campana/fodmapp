---
"@fodmap/design-tokens": patch
---

Track `FD_BUILD_TARGET` and `FD_OUTPUT_PATH` in Turborepo build hashing for `@fodmap/design-tokens` to prevent stale cache replays across target/output variants.
