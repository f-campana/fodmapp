---
"@fodmap/reporting": patch
---

Migrate scientific SVG rendering colors to semantic/reporting CSS variables, keep deterministic theme fallbacks in reporting styles, and add a regression check that blocks raw hex literals in the scientific renderer.
