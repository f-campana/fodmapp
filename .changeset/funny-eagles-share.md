---
"@fodmapp/api-client": patch
"@fodmapp/app": patch
---

Extract the first production-grade `@fodmapp/api-client` package for domain-backed public catalog reads and move the app's foods search/detail plus swaps flows onto that shared client boundary.
